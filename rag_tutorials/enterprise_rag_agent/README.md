# Enterprise RAG Agent Workspace

项目分为两部分：
- `backend/`：企业级 RAG 后端
- `frontend/`：Vue 3 + Vite + TypeScript + Pinia + Router 前端

## 运行

后端：
```bash
cd backend
pip install -r requirements.txt
python app.py serve
```

前端：
```bash
cd frontend
npm install
npm run dev
```
